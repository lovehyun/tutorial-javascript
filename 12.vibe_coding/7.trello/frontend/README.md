# TaskFlow Frontend (React + Vite)

## Install
```bash
cd frontend
npm i
```

## Run
```bash
npm run dev
```

- 기본 API 주소: `http://localhost:3001/api/v1`
- 필요하면 `.env`로 변경:
  - `VITE_API_BASE=http://localhost:3001/api/v1`

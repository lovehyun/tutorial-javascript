# CRM · 03_single_table_crud — CRUD + 트랜잭션

## 이 파트에서 배우는 것
- INSERT/UPDATE/DELETE
- 안전한 WHERE
- 트랜잭션(COMMIT/ROLLBACK)

## 실무 질문
- 새 상품을 등록/가격 수정/삭제하는 기본 CRUD는?
- 실수로 UPDATE 했을 때 되돌리는 방법(ROLLBACK)은?

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read crm/03_single_table_crud/(파일명).sql
```

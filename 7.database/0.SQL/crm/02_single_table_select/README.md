# CRM · 02_single_table_select — 단일 테이블 SELECT 마스터

## 이 파트에서 배우는 것
- WHERE/LIKE/ORDER BY/LIMIT
- DISTINCT/NULL/COALESCE
- 표현식/별칭

## 실무 질문
- 서울에 사는 고객만 보고 싶어요.
- 상품 가격이 비싼 TOP 5를 뽑고 싶어요.
- 최근 주문 5개만 보고 싶어요(페이지네이션 포함).

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read crm/02_single_table_select/(파일명).sql
```

# CRM · 07_subquery_basic — 서브쿼리 기본

## 이 파트에서 배우는 것
- 스칼라/IN/EXISTS
- 서브쿼리의 반환 형태 이해
- JOIN과 비교

## 실무 질문
- 평균 주문금액보다 큰 주문만 보고 싶어요.
- 주문이 있는 고객만/없는 고객만 보고 싶어요(EXISTS/NOT EXISTS).

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read crm/07_subquery_basic/(파일명).sql
```

# CRM · 04_aggregation — 집계/그룹핑

## 이 파트에서 배우는 것
- COUNT/SUM/AVG
- GROUP BY/HAVING
- Top-N/비율/버킷팅

## 실무 질문
- 우리 매장별 주문 수는?
- 재구매(2회 이상) 고객은?
- 연령대(버킷)별 고객 수는?

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read crm/04_aggregation/(파일명).sql
```

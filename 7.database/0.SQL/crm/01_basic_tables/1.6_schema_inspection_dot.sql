-- 목적: sqlite3 CLI에서 자주 쓰는 확인 명령(주석)
-- 아래는 SQL이 아니라 sqlite3 '점(.) 명령'입니다.

-- .tables           -- 테이블 목록
-- .schema users     -- 특정 테이블 CREATE 문 보기
-- .headers on
-- .mode column
-- .nullvalue NULL
-- .read 파일.sql     -- 파일 실행

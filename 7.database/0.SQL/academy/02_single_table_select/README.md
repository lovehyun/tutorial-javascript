# Academy · 02_single_table_select — 단일 테이블 SELECT 마스터

## 이 파트에서 배우는 것
- WHERE/LIKE/ORDER BY/LIMIT
- DISTINCT/NULL/COALESCE
- 표현식/별칭

## 실무 질문
- 2학년 이상 학생만 보고 싶어요.
- 재직연수 TOP 교수는?

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read academy/02_single_table_select/(파일명).sql
```

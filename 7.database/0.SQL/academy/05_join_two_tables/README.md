# Academy · 05_join_two_tables — JOIN 2개 테이블

## 이 파트에서 배우는 것
- INNER/LEFT JOIN
- ON vs WHERE 위치
- JOIN + 집계

## 실무 질문
- 학생+학과 JOIN
- 과목+교수 JOIN
- 수강+성적 LEFT JOIN(성적 없는 케이스)

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read academy/05_join_two_tables/(파일명).sql
```

# Academy · 07_subquery_basic — 서브쿼리 기본

## 이 파트에서 배우는 것
- 스칼라/IN/EXISTS
- 서브쿼리의 반환 형태 이해
- JOIN과 비교

## 실무 질문
- 평균 점수보다 높은 성적만
- A 받은 학생만(IN)

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read academy/07_subquery_basic/(파일명).sql
```

# Academy · 06_join_three_tables — JOIN 3~4개 테이블

## 이 파트에서 배우는 것
- 수강(enrollments) 조인 감각
- 성적(grades) LEFT JOIN
- 정렬/표 형태 결과

## 실무 질문
- 학생별 수강 목록
- 학생별 성적표(LEFT 포함)

## 실행 방법
1) DB 초기화 후 sqlite3 접속
2) 아래처럼 실행
```sql
.read academy/06_join_three_tables/(파일명).sql
```

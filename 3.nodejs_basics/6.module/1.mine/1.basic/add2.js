// 모듈 파일 (예: operations.js)
function addNumbers(a, b) {
    return a + b;
}

function subtractNumbers(a, b) {
    return a - b;
}

// 여러 개의 함수를 내보내기
module.exports = {
    addNumbers,
    subtractNumbers
};

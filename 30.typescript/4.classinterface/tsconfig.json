{
    "compilerOptions": {
        "target": "ES2020",               // Node.js에서 사용하는 최신 JavaScript 문법 지원
        "module": "CommonJS",             // Node.js는 CommonJS 모듈 시스템을 사용
        "outDir": "./dist",               // 컴파일된 JavaScript 파일 출력 디렉터리
        "rootDir": "./src",               // TypeScript 소스 파일이 위치한 디렉터리
        "strict": true,                   // 엄격한 타입 검사 활성화
        "esModuleInterop": true,          // CommonJS 및 ES6 모듈 호환성을 보장
        "forceConsistentCasingInFileNames": true, // 파일 이름 대소문자 일관성 유지
        "skipLibCheck": true,             // 라이브러리 타입 검사 생략 (컴파일 속도 향상)
        "moduleResolution": "node",       // Node.js 방식으로 모듈을 탐색
        "resolveJsonModule": true,        // JSON 파일 import 지원
        "allowSyntheticDefaultImports": true, // ES6 모듈의 기본 import 지원
        "noImplicitAny": true,            // 암시적인 `any` 타입 금지
        "noImplicitReturns": true,        // 모든 함수에서 명시적인 반환값 필요
        "noUnusedLocals": true,           // 사용되지 않는 지역 변수 금지
        "noUnusedParameters": true        // 사용되지 않는 함수 매개변수 금지
    },
    "include": ["src/**/*"],            // 포함할 파일 경로
    "exclude": ["node_modules", "dist"] // 제외할 디렉터리
}
